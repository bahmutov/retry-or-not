{
  "name": "retry-or-not",
  "version": "1.0.0",
  "description": "How to dynamically determine if a Cypress test should be retried or not based on the error",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Gleb Bahmutov",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.14.0",
    "prettier": "^2.8.6"
  }
}
